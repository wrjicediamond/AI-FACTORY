# 电子信息制造业企业智能化项目核心功能流程图 v2

> 说明：本流程图用于展示系统核心功能闭环。所有连线均为实线箭头，不使用虚线。

```mermaid
flowchart TD
    A["企业业务背景<br/>生产、质量、设备、成本、排产等场景存在智能化需求"]
    B["用户端：提出需求<br/>选择业务模板、填写需求、上传附件"]
    C["流程中心：需求入池<br/>生成需求编号、记录状态、分配处理人"]
    D["开发端：需求整理<br/>生成需求.md、拆解业务目标、指标、数据和规则"]
    E["用户端：需求确认<br/>确认需求摘要、补充资料、退回修改"]
    F["流程中心：评估立项<br/>评估价值、难度、数据可得性、实施优先级"]

    G["开发端：数据评估<br/>识别 MES、ERP、SCADA、质量系统、Excel 等数据来源"]
    H["开发端：数据接入<br/>配置 API、数据库、同步库、文件导入、人工录入"]
    I["开发端：数据目录<br/>配置表、字段、业务注释、单位、敏感级别"]
    J["权限中心：权限配置<br/>配置租户、角色、API、数据源、表、字段、工具、智能体权限"]

    K["开发端：规则配置<br/>配置系统规则和用户规则，包括指标口径、查询口径、校验规则"]
    L["开发端：工具配置<br/>配置 SQL 工具、HTTP 工具、Rule 工具"]
    M["开发端：智能体配置<br/>绑定模型、提示词、数据范围、规则、工具、授权用户"]
    N["Workspace：沙箱测试<br/>模拟用户权限，测试正常、异常、越权、边界和输出结果"]
    O["管理端：发布审核<br/>检查数据范围、权限范围、规则版本、工具测试、沙箱结果"]

    P["用户端：使用智能体<br/>通过 Web、飞书、钉钉发起问题"]
    Q["智能体运行中心：任务创建<br/>生成 trace_id，校验用户和智能体权限"]
    R["智能体运行中心：权限快照<br/>计算用户权限与智能体绑定范围的交集"]
    S["智能体运行中心：安全上下文<br/>只注入授权数据目录、规则和工具"]
    T["智能体运行中心：执行任务<br/>调用模型、规则和授权工具"]
    U["智能体运行中心：输出检查<br/>AnswerGuard 检查越权字段、敏感信息和输出格式"]
    V["用户端：查看结果<br/>展示结论、依据、规则口径、权限说明、trace_id"]
    W["用户端：反馈验收<br/>提交满意度、准确性、补充意见和验收意见"]
    X["交付沉淀<br/>形成交付报告、测试报告、规则模板、工具模板、智能体模板"]

    A --> B
    B --> C
    C --> D
    D --> E
    E --> F
    F --> G
    G --> H
    H --> I
    I --> J
    J --> K
    K --> L
    L --> M
    M --> N
    N --> O
    O --> P
    P --> Q
    Q --> R
    R --> S
    S --> T
    T --> U
    U --> V
    V --> W
    W --> X
    X --> B

    E --> D
    N --> I
    N --> K
    N --> L
    N --> M
    O --> M
```

## 流程说明

| 阶段 | 核心功能 | 主要产出 |
| --- | --- | --- |
| 需求阶段 | 用户提需求、流程入池、开发端整理、用户确认 | 原始需求、需求.md、确认后的需求摘要 |
| 评估阶段 | 价值评估、难度评估、数据可得性评估 | 立项结果、实施任务 |
| 数据阶段 | 数据评估、数据接入、数据目录、字段注释 | 数据源配置、数据目录、字段敏感级别 |
| 权限阶段 | 租户、角色、API、数据、字段、工具、智能体授权 | 有效权限、权限预览、权限审计 |
| 能力配置阶段 | 规则、工具、智能体配置 | 规则版本、工具版本、智能体版本 |
| 测试发布阶段 | Workspace 沙箱测试、发布审核 | 测试报告、发布记录 |
| 运行阶段 | 用户提问、权限快照、安全上下文、模型和工具调用、输出检查 | 智能体回答、trace_id、运行审计 |
| 反馈沉淀阶段 | 用户反馈、验收、交付报告、模板沉淀 | 交付报告、可复用规则/工具/智能体模板 |

